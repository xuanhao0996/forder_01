package com.framgia.controller;

public class UsersController {
	
/*	@Autowired
	private UserService userService;
	
	
	public UserInfo getUserByEmail(HttpSession httpSession, Authentication authentication) {
		String emailCurrent =  (String) httpSession.getAttribute("email");
		UserInfo userInfo =  userService.findByEmail(authentication.getName());
		System.out.println(emailCurrent);
		httpSession.setAttribute("user",userService.findByEmail(emailCurrent));
		return userService.findByEmail(emailCurrent);
	}*/
}
